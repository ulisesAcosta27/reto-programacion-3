const multiplosDeOcho = []
const numeroOcho = 8;
let inicio = 1;

while (inicio <= 500) {
    const multiplicacion = inicio * numeroOcho;
    multiplosDeOcho.push(multiplicacion)
    inicio++;
}

console.log(multiplosDeOcho);